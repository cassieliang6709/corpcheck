# ── Data (share via Google Drive, not git) ───────────────────────────────────
data/
sec-edgar-filings/
*.jsonl
*.npy
*.pgdump
*.csv

# Raw SEC filing formats
*.htm
*.html
*.xml
*.xbrl

# Logs
logs/
*.log

# ── Python ───────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
*.egg
.venv/
.venv-py39-backup/
venv/
env/

# ── Jupyter ──────────────────────────────────────────────────────────────────
.ipynb_checkpoints/
*.ipynb_checkpoints

# ── Secrets / env ────────────────────────────────────────────────────────────
secrets.json
config_local.py

# ── OS ───────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Project-local ────────────────────────────────────────────────────────────
.venv/
.env
evaluation/runs/
# Benchmark run artifacts: reproduced from a command, not source of truth.
# (evals/ moved out with the GSM8K track; official runs land in
#  evaluation/official/runs/, which is deliberately NOT ignored.)
evaluation/runs/*/queries.jsonl
docker/financial_rag.dump
.playwright-cli/
output/playwright/

# The *.html rule above targets downloaded SEC filings, not the landing page.
!landing/*.html
!landing/en/
!landing/en/*.html
